package com.elearning.dao;

import com.elearning.dtos.AdminResponseDTO;
import com.elearning.pojos.Users;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.Query;
import org.springframework.stereotype.Repository;
import java.util.List;

@Repository
public interface AdminDAO extends JpaRepository<Users, Long> {

    @Query("SELECT new com.elearning.dto.AdminResponseDTO(DATE_FORMAT(u.registrationDate, '%Y-%m'), COUNT(u.userId)) \r\n"
    		+ "FROM Users u \r\n"
    		+ "GROUP BY DATE_FORMAT(u.registrationDate, '%Y-%m') \r\n"
    		+ "ORDER BY DATE_FORMAT(u.registrationDate, '%Y-%m') ASC\r\n"
    		+ "")
    List<AdminResponseDTO> getUserCountPerMonth();
}
